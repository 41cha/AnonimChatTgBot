# Telegram bot token from @BotFather
BOT_TOKEN=

# MongoDB Atlas connection string (e.g. mongodb+srv://user:pass@cluster.mongodb.net/anonbot)
MONGODB_URI=

# Secret token for webhook verification (any random string, e.g. generate with: openssl rand -hex 32)
WEBHOOK_SECRET=
